# Claude Code worktrees. These are full working copies of this repository
# (currently ~1.2 MB); committing one would nest a second copy of the repo
# inside itself. Anything else under .claude/ stays trackable on purpose.
.claude/worktrees/
